# ============================================================
# Test configuration
# Provides required properties for @WebMvcTest slices that
# import SecurityConfig (@EnableConfigurationProperties(JwtProperties.class)).
# Values are non-sensitive placeholders safe to commit.
# ============================================================

security.jwt.secret=test-secret-for-ci-that-is-at-least-32-chars-long
security.jwt.expiration-minutes=120
security.jwt.refresh-expiration-days=7
